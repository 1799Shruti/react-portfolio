What us Browser Routing?

    This Component enables client side routing using the browser's  history API

Routes?

    It's responsible for renderring appropriate components based on the current URL

Route?

    Each route component defines a path and the corresponding component to render when that path is matched

Link?

    Its similar to href. A link is useful to navigate from one page to another.

Dynamic Routing?
